# Modo de la API
#   mock  → datos locales de demo (default, no requiere backend)
#   proxy → llama al proxy de Node que conecta a Creatio
VITE_API_MODE=mock

# URL del proxy (solo se usa cuando VITE_API_MODE=proxy)
VITE_PROXY_URL=http://localhost:3000
